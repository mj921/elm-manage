import { Icon } from "antd";

export default function() {
  return (
    <div style={{ textAlign: "center", color: "#1b8cf0" }}>
      <Icon
        type="loading"
        style={{ fontSize: "36px", verticalAlign: "middle" }}
        spin />
    </div>
  );
}